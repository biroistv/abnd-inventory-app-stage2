package com.example.biro.inventoryapp.state;
/*
 *   EMPTY - there is no character in the string
 *   INVALID - the string is not a valid data
 *   VALID - the string is valid
 * */
public enum State {
    EMPTY,
    INVALID,
    VALID
}

